import * as svc from '../services/api';

export const api = svc.api;
export const authLogin = svc.authLogin;
export const authRegister = svc.authRegister;
export const apiFetch = svc.apiFetch;

export default api;
